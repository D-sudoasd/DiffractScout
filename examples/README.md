# Examples

`demo_cifs/` contains synthetic validation fixtures. They are not experimental measurements and must not be cited as material-property data.

Run:

```bash
diffractscout analyze examples/demo_cifs -o outputs/demo
```
